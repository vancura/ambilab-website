<script lang="ts">
    import { SITE } from '@config/site';

    interface SocialLink {
        name: string;
        url: string;
        icon: string;
    }

    const socialLinks: SocialLink[] = [
        { name: 'X (Twitter)', url: SITE.SOCIAL.X, icon: '𝕏' },
        { name: 'Threads', url: SITE.SOCIAL.THREADS, icon: '◈' },
        { name: 'Instagram', url: SITE.SOCIAL.INSTAGRAM, icon: '📷' },
        { name: 'LinkedIn', url: SITE.SOCIAL.LINKEDIN, icon: 'in' },
        { name: 'Mastodon', url: SITE.SOCIAL.MASTODON, icon: '🐘' },
        { name: 'Bluesky', url: SITE.SOCIAL.BLUESKY, icon: '🦋' },
        { name: 'GitHub', url: SITE.SOCIAL.GITHUB, icon: 'GH' },
    ];
</script>

<div class="flex flex-wrap gap-3">
    {#each socialLinks as link (link.url)}
        <a
            href={link.url}
            target="_blank"
            rel="noopener noreferrer"
            class="flex h-9 w-9 items-center justify-center rounded-lg border border-gray-200 text-sm font-medium transition-colors hover:bg-gray-100 dark:border-gray-800 dark:hover:bg-gray-800"
            aria-label={link.name}
            title={link.name}
        >
            {link.icon}
        </a>
    {/each}
</div>
