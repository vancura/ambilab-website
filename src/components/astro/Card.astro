---
export interface Props {
    title?: string;
    class?: string;
}

const { title, class: className = '' } = Astro.props as Props;
---

<div class:list={['border-border-default dark:border-border-default-dark border-2 p-6 transition-all', className]}>
    {title && <h3 class="mt-0 text-xl">{title}</h3>}

    <div>
        <slot />
    </div>
</div>
